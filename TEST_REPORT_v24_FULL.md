# MARU SPORTS v24 FULL TEST REPORT

- py_compile_app: PASS 
- app_version_v24: PASS 
- mobile_url_constant: PASS 
- pc_panel_function: PASS 
- pc_panel_called_before_metrics_in_main: PASS 
- copy_button_js: PASS 
- manual_text_box: PASS 
- qr_function: PASS 
- mobile_real_buttons: PASS 
- button_state_toggle: PASS 
- st_button_no_width_stretch: PASS ['        if st.button("🚀 전체 실행 + 허브 전송", type="primary", use_container_width=True):', '        if st.button("📱 모바일 추천 보기", use_container_width=True):', '        if st.button("📊 구글시트 열기", use_container_width=True):', '        if st.button("🧪 고급 진단", use_container_width=True):', '        if st.button("📡 오늘~7일 일정 자동수집", type="primary", use_container_width=True):', '        if st.button("🔗 자동 매칭 실행", type="primary", use_container_width=True):', '        if st.button("🚀 자동수집+매칭+분석", type="primary", use_container_width=True):', '        if st.button("프로토 보완 저장", key="save_proto_ticket"):', '        if st.button("일정 보완 저장", key="save_livescore_schedule"):', '    if st.button("manual 현재자료 저장", type="primary"):', '        if st.button("📤 현재 결과 허브 전송/큐 저장", type="primary"):', '        if st.button("🧪 허브 dry-run payload 생성"):', '        if st.button("✅ 허브 실제 전송 테스트"):', '        if st.button("🧪 가상 백엔드 전체 테스트", type="primary"):', '        if st.button("📋 전체 기본 검사표 생성"):', '        if st.button("🐒 Sportmonks API 단독 테스트"):', '    if st.button(label, key=f"btn_{state_key}", type=button_type, use_container_width=True):']
- use_container_width_buttons: PASS 
- no_auto_buy_payment: PASS 